{ "version": 2, 
"env": {
    "NODE_OPTIONS": "--openssl-legacy-provider"
  },
 "builds":[
   {
    "src": "index.js",
    "use": "@now/node-server"
   } 
 ],
 "routes": [
   {
     "src": "/(.*)",
     "dest": "/index.js"
   }
 ]
}